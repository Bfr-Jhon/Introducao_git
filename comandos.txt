git init -> cria o repositório local

git remote add origin 'link do GitHub'

git add . 

git commit -m " mensagem do commit"

git push origin main


*agora é uma nova branch de testes

git checkout -b dev